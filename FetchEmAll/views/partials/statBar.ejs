<script>
    console.log(`<%poke1.hp%>`)
    async function dynamicStats() {
        // event.preventDefault();
        console.log()
        const hpBar = document.getElementById("testing")
        hpBar.style.setProperty("width", `<%50/100%>%`)
        console.log(`<%poke1.hp%>`)
        console.log(hpBar.style)
    }
</script>